pdfplumber>=0.10
playwright>=1.40
python-dotenv>=1.0
streamlit>=1.40
pandas>=2.0
python-docx>=1.1
fpdf2>=2.7
pytest>=8.0

# AI mode only — Standard mode needs none of this. The app runs, and every
# feature works, with these absent; they are imported lazily.
anthropic>=0.69   # the Claude provider
httpx>=0.27       # the OpenAI-compatible provider (Ollama, LM Studio, OpenAI)
