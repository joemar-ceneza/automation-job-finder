pdfplumber>=0.10
playwright>=1.40
python-dotenv>=1.0
streamlit>=1.40
pandas>=2.0
python-docx>=1.1
fpdf2>=2.7
pytest>=8.0

# Optional extras — imported lazily, so the app runs and every core feature
# works with these absent.
anthropic>=0.69   # AI mode: the Claude provider
httpx>=0.27       # AI mode: the OpenAI-compatible provider (Ollama, LM Studio,
                  #   OpenAI). Also used by the Telegram notification channel.
